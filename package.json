{
  "name": "javascrip",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "server": "json-server --watch server/db.json --port 3001",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "parcel": "^2.12.0"
  }
}
